## 2. Usage

### Prerequisites

#### **System Requirements**
- Windows 10/11 (or macOS/Linux with minor adjustments)
- Python 3.8 or higher
- 8GB RAM minimum (16GB recommended)
- 5GB disk space for Ollama + models

#### **Background Experience**
This project builds on previous work:
- **Dec 10-11**: Lyrics data pipeline (prototype_1210, prototype_1211)
- **Dec 12**: First embedding experiments
- **Dec 15**: 3D visualization prototypes
- **Dec 16-23**: Refinement into production system

See `record/chat_record/PROJECT_JOURNEY.md` for complete development history.

#### **Software Dependencies**
1. **Python packages** (installed in virtual environment):
   ```
   sentence-transformers==3.3.1
   numpy==2.3.5
   torch==2.9.1+cpu
   plotly==6.5.0
   scikit-learn==1.8.0
   requests==2.32.5
   ```

2. **Ollama** (desktop application):
   - Download: https://ollama.com/download
   - Or install via: `winget install Ollama.Ollama`

### Installation

#### **Step 0: Understanding the Journey**

If you're new to this project, you might want to see how it evolved:

```powershell
# Explore early prototypes (optional)
cd "Final Project/prototype_1210"  # Lyrics pipeline
cd "Final Project/prototype_1211"  # Early embeddings

# These folders show the learning process:
# - 1211_prototype.py: First experiments
# - 1212_embedding.py: Working embeddings
# - 1215_embedding3D.py: First 3D visualizations
# - 1215-2_embedding3D.py: Refined version
```

The current `AI-dialogue-project/` is the culmination of this work.

#### **Step 1: Install Ollama**
```powershell
# Method 1: Using winget
winget install Ollama.Ollama

# Method 2: Download from https://ollama.com/download
```

#### **Step 2: Pull AI Model**
```powershell
# Download llama3.2 (2GB)
ollama pull llama3.2

# Verify installation
ollama list
```

#### **Step 3: Set Up Python Environment**
```powershell
# Navigate to CURRENT project directory (not prototypes!)
cd "C:\Users\C\OneDrive\Desktop\計算機程式設計\code\114-1\AI-dialogue-project"

# Activate virtual environment
.\.venv\Scripts\Activate.ps1

# Verify correct environment
python -c "import sys; print(sys.executable)"
# Should show: ...AI-dialogue-project\.venv\Scripts\python.exe

# Verify packages (should already be installed)
pip list
```

### Development Timeline Reference

**What to run based on project stage:**

| Date | File | Purpose | Status |
|------|------|---------|--------|
| Dec 10-11 | `prototype_1210/V2_scripts_lyrics_pipeline.py` | Learning data pipelines | Historical |
| Dec 12 | `prototype_1211/1212_embedding.py` | First working embeddings | Historical |
| Dec 15 | `prototype_1211/1215-2_embedding3D.py` | Static visualization | Historical |
| Dec 22 | `core/1222-1_embedding3D.py` | Gemini integration | Deprecated |
| Dec 23 | `core/1223_fast_live_dialogue.py` | **CURRENT VERSION** | ✅ Use This |

**Important**: Always use `core/1223_fast_live_dialogue.py` for actual work.  
The older files are preserved for learning and reference.

### Running the Program

#### **Current Production Version** ⭐

```powershell
# Make sure you're in AI-dialogue-project and venv is activated
python core/1223_fast_live_dialogue.py
```

#### **What Happens**
1. ⏳ **Loading phase** (~15 seconds):
   - Loads SentenceTransformer model (~5 sec)
   - Pre-warms Ollama (~10 sec)
   - This delay only happens once per session!

2. 💬 **Chat interface**:
   ```
   💬 Your message: Hello, what is AI?
   ```

3. 🤖 **Ollama responds**:
   ```
   OLLAMA: Artificial Intelligence refers to...
   ```

4. 📊 **Graph opens** (after 3+ sentences):
   - Browser opens automatically
   - 3D scatter plot appears
   - Auto-refreshes every 3 seconds

5. 🔄 **Continue chatting**:
   - Keep typing messages
   - Watch points appear in real-time
   - Graph updates automatically

... (rest of usage section remains the same)